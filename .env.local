############################################################
# Frontend (Vite) environment variables
############################################################

# Base URL for the deployed API (no trailing slash)
VITE_CLOUD_RUN_BASE_URL=https://api.doribharat.com

# Origin of the deployed frontend (used for CORS headers in requests)
VITE_FRONTEND_ORIGIN=https://www.doribharat.com

# Base URL for the media signing server
VITE_MEDIA_API_BASE=http://localhost:4000

# Gemini / Generative AI API key (used in services/geminiService.ts)
GEMINI_API_KEY=your-gemini-api-key
